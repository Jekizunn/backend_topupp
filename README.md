# Backend API - Top Upp

Ini adalah server-side untuk aplikasi Top Upp, dibuat menggunakan PHP Native.

## 📂 Isi File
* `submit.php`: Endpoint untuk menerima transaksi top up.
* `history.php`: Endpoint untuk melihat riwayat transaksi.
* `koneksi.php`: Konfigurasi koneksi database.

## ⚙️ Cara Instalasi (Untuk Penilai)

1.  Download source code ini.
2.  Simpan di folder `htdocs` (XAMPP) atau `www` (Laragon).
3.  Import file `db_topup.sql` ke MySQL (phpMyAdmin/HeidiSQL).
4.  Sesuaikan username/password database di file `koneksi.php` jika perlu.
5.  Pastikan IP Address di aplikasi Flutter sudah sesuai dengan IP server ini.
